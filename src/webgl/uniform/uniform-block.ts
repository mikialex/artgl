import { GLProgram } from "../program";
import { UniformBlockDescriptor } from "../interface";

export class GLUniformBlock{
  constructor(program: GLProgram, descriptor: UniformBlockDescriptor) {
    
  }

  set(ubo: WebGLBuffer) {
    
  }
}