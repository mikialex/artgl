import { StandardGeometry } from "../standard-geometry";

export function combineStandardGeometry(
  geoA: StandardGeometry,
  geoB: StandardGeometry,
): StandardGeometry {
  return new StandardGeometry();
}